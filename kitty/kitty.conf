# ~/.config/kitty/kitty.conf
# Modular Aesthetic Terminal Configuration
# Themes, keybindings, and sessions are split into separate files.

# ── 1. FONTS & TYPOGRAPHY ──────────────────────────────────
font_family      JetBrainsMono Nerd Font
bold_font        JetBrainsMono Nerd Font Bold
italic_font      JetBrainsMono Nerd Font Italic
bold_italic_font JetBrainsMono Nerd Font Bold Italic
font_size        14.5
adjust_line_height  115%
adjust_column_width 100%
disable_ligatures   cursor
box_drawing_scale   0.001, 1, 1.5, 2
# Text composition strategy ensures sharp font edges with zero muddy antialiasing
text_composition_strategy 1.8 35
dim_opacity         0.90

# ── 2. CURSOR & ANIMATIONS ────────────────────────────────
cursor_shape           beam
cursor_beam_thickness  1.5
cursor_shape_unfocused hollow
# Fluid subtle animated cursor trail
cursor_trail 1
cursor_trail_decay 0.1 0.25
cursor_trail_start_threshold 2
cursor_blink_interval 0.5
cursor_stop_blinking_after 15.0

# ── 3. WINDOW & LAYOUT ────────────────────────────────────
window_padding_width        16 20
window_margin_width         0
single_window_margin_width  -1
window_border_width         1.5pt
draw_minimal_borders        no
hide_window_decorations     no
enabled_layouts              splits,tall,stack,fat,grid,horizontal,vertical
remember_window_size        yes
confirm_os_window_close     0

# ── 4. BACKGROUND & TEXTURE ────────────────────────────────
background_opacity          1.0
background_blur             0
dynamic_background_opacity  no
background_image            /home/kanishk/.config/kitty/textures/matte-stipple.png
background_image_layout     tiled
background_tint             0.15
background_image_linear     yes
inactive_text_alpha          1.0

# ── 5. TAB BAR (Aesthetic Powerline & Crisp Layout) ─────────
tab_bar_edge          top
tab_bar_style         powerline
tab_powerline_style   slanted
tab_bar_min_tabs      2
tab_title_max_length  20
tab_title_template    " {index} {tab.active_wd.split('/')[-1] if tab.active_wd else title} "
active_tab_font_style   bold
inactive_tab_font_style normal

# ── 6. CLIPBOARD & MOUSE ──────────────────────────────────
copy_on_select        yes
strip_trailing_spaces smart
mouse_hide_wait       3.0
pointer_shape_when_grabbed hand
mouse_map right press ungrabbed paste_from_clipboard
clipboard_control     write-clipboard write-primary no-append

# ── 7. URL HANDLING ────────────────────────────────────────
url_style               curly
detect_urls             yes
open_url_with           default

# ── 8. SCROLLBACK & PAGER ─────────────────────────────────
scrollback_lines        20000
scrollback_pager        less --chop-long-lines --RAW-CONTROL-CHARS +INPUT_LINE_NUMBER
wheel_scroll_multiplier 3.0

# ── 9. BELL & NOTIFICATIONS ───────────────────────────────
enable_audio_bell     no
visual_bell_duration  0.15
visual_bell_color     #f9e2af
window_alert_on_bell  yes
command_on_bell       none
notify_on_cmd_finish  invisible 15.0

# ── 10. PERFORMANCE & INTEGRATION ─────────────────────────
repaint_delay         6
input_delay           1
sync_to_monitor       yes
update_check_interval 0
shell_integration     enabled
shell                 .

# ── 11. REMOTE CONTROL (enables theme hot-swap & broadcast) ──
allow_remote_control    socket-only
listen_on               unix:/tmp/kitty-{kitty_pid}

# ── 12. INCLUDES (modular config) ─────────────────────────
include theme.conf
include keybindings.conf

